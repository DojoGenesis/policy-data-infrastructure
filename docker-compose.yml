services:
  postgres:
    image: postgis/postgis:16-3.4
    environment:
      POSTGRES_DB: pdi
      POSTGRES_USER: pdi
      POSTGRES_PASSWORD: pdi
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
