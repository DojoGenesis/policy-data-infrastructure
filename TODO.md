# TODO — policy-data-infrastructure

> Updated: 2026-08-08 | Last audit: 2026-07-28 (frontend rebuild, 4 parallel tracks, PIP-115)
> Update this file after every work session. Move completed items to CHANGELOG.md.

## P1 — Follow-ups from the 2026-08-08 explorer truth pass

- [x] ~~**County-level factor scores need a defensible method ruling.**~~ — RESOLVED:
  ADR-015 **ratified by operator 2026-08-08**. Population-weighted rollup through the
  run queue (CI + coverage recorded, withheld under threshold, '-popw' vintage rows)
  is the standing method [source: `10cb81f`]
- [ ] **County page time-series fetch requests bare-year vintages** (`vintage=2023,2022,...`)
  which match nothing since vintage strings became `ACS-2024-5yr`-style — the sparkline
  section has been silently empty and stays so. Wire it to real vintage strings or drop
  it until multi-vintage data exists [source: network audit 2026-08-08]
- [ ] **LISA into the run queue** — still Python-only, off-DB (also listed below); the
  launch warm covers everything queue-borne EXCEPT it. Candidate: a Python worker
  consuming `analysis_runs` rows of type `lisa` [source: warm design]

## P1 — Follow-ups from the 2026-08-08 run-layer build (handoff 2026-08-02)

- [ ] **Route `composite_index` and `correlation` through POST /analyses.** The queued-run
  API ships with six types; the two the CLI already computes still require shell access,
  which is the exact gap ADR-014 §"Analyses cannot be created through the API" describes.
  The executor registry makes this mechanical now (reuse analyze.go's logic) [source: run-layer build]
- [ ] **LISA remains Python-only and reads a static GeoJSON, not the DB** (ADR-014 D11's
  last clause). Until it moves onto Postgres through `store.Store`, the map's analyses
  describe a different dataset than the API's. Larger lift: needs spatial weights in Go
  or a Python worker consuming the run queue [source: ADR-014 F5]
- [ ] **Crashed-run recovery is deliberately manual.** A worker that dies mid-run leaves
  its row 'running' forever; the runner never re-claims it (re-executing a half-written
  analysis would double-write scores). Add an operator query/runbook line, or a
  stale-running sweep with an explicit age threshold [source: runner design]
- [ ] **Frontend surfaces nothing from the run API yet.** The composites page could offer
  "compute county rollup" against POST /analyses and render the CI + coverage that
  tract_rollup returns — the D8 uncertainty display exists in the API and nowhere else
  [source: run-layer build]

## P0 — Blocking correctness (found 2026-07-28)

- [x] ~~**`/evidence` and `/candidates` rendered as VISUALLY BLANK pages.**~~ — DONE 2026-07-29. 70 of 71 cards sat at `opacity: 0` permanently, even after a full scroll; the content was in the DOM (~19,875 chars on /evidence) but invisible. **Two independent causes.** (1) `motion.js`'s `reveals()` queries `.reveal` once at boot and observes only what exists then — Alpine renders the ~70 cards via `x-for` *after* that, so those nodes were never observed and never got `.in`. Fixed by re-calling `reveals()` in a `$nextTick` after load, the pattern already precedented at `compare.html:2518` and `county.html:2224`. (2) One JS error per page killed Alpine mid-render: `candidates.html:576` referenced an `idx` its `x-for` never declared, and `evidence.html:712` dereferenced `top_need_counties[0].geoid` where exactly 1 of 70 cards has that array empty — `x-show` gates visibility only, Alpine still evaluates `:href` on hidden elements. Verified 0 hidden / 0 pageerrors on both pages in normal and reduced-motion modes [source: Track A]

- [x] ~~**Reveal stagger delay compounded to 5.5s on long lists.**~~ — DONE 2026-07-29. `--d` feeds motion.css's `transition-delay: calc(var(--d,0) * 1ms)` at an 80ms step. On flat lists using raw `idx * 80`, the last card's fade-in did not *start* until 5.52s after being marked `.in` — visually indistinguishable from the blanking bug above. Capped to `idx % 12` on `evidence.html` (grid) and `idx % 8` on `composites.html:915` (single-column stack, reaches 72 rows at county scope). `candidates.html:576` was left at raw `idx` because its index resets per equity dimension and is therefore already bounded [source: Track A + orchestrator]


- [x] ~~**Generated narrative `<h1>` reads "Five Mornings in " — the county name is missing.**~~ — DONE 2026-07-29. Root cause was **not** the template engine: `pkg/gateway/handlers.go` built `narrative.GenerateRequest` without ever setting `ScopeName`, so `defaultTitle()` concatenated an empty string. The CLI path (`cmd/pdi/generate.go`) set it correctly, which is why this reproduced only through the API. Fixed by adding `resolveScopeName()` (GEOID → `store.GetGeography`, falling back to the analysis record's `ScopeGEOID`, then to the GEOID itself, then to a generic label), wired into **both** `handleGenerateNarrative` and `handleServeNarrative` — the latter had the identical bug and was not in the original report. `Engine.Generate` now also normalizes a blank `ScopeName` so every `{{.ScopeName}}` interpolation is covered, not just the title. 5 regression tests added, verified to fail against the old code [source: Track D]
- [ ] **`lib/chat.js`, `lib/api.js`, `lib/domain.js` export via top-level `const`, not `window`.** In a classic script a top-level `const` is a script-scoped lexical binding, never a property of `window` — so `window.ChatAdapter`/`window.PDI` are permanently `undefined` while the bare identifiers work. This silently broke chat's Send button and Enter key for an unknown period (7 guard sites in `chat.html`, all fixed). Any future page reaching for these through `window` hits the same trap. Either assign them to `window` explicitly in the lib files, or document the bare-identifier contract at the top of each [source: chat track, verified live]

- [ ] **`fcc_broadband.go` FetchState/FetchCounty `return nil, nil` — a SILENT EMPTY SUCCESS.** The pipeline records success and loads zero rows, with no error anywhere. This directly violates the repo's own rule (CLAUDE.md, Architecture Patterns): *"Return HTTP 501 for unimplemented interface methods — makes gaps visible, not silent."* Return an explicit unimplemented error instead [source: sources.toml audit, verified at `pkg/datasource/fcc_broadband.go:107`]
- [ ] **CDC PLACES app-token env var mismatch.** The Go adapter reads `CDC_PLACES_APP_TOKEN`; `ingest/fetch_cdc_places.py` reads `SOCRATA_APP_TOKEN`. Setting only one leaves the other path silently unauthenticated — and Socrata degrades to a low anonymous rate limit rather than erroring, so it presents as intermittent slowness, not auth failure. Pick one name or read both [source: verified in both files]
- [x] ~~**CDC SVI registered but never loaded (5 variables, 0 rows).**~~ — DONE 2026-07-30. Root cause was a dead host, not a code bug: every path under `https://svi.cdc.gov/Documents/Data` 404s. CDC moved downloads to a React app on **svi2.cdc.gov** serving a query-parameter webapi (`/webapi/Documents/download?year=&type=csv&category=&name=`), found by extracting the URL construction from the app bundle. **Trap worth knowing: svi2.cdc.gov is an SPA that returns 200 with its HTML shell for ANY path** — four candidate URLs all "succeeded" while returning HTML. Same shape as the Census keyless-redirect trap; verify by Content-Type and by parsing a row, never by status. Fetched per state deliberately — within a state download, geographies are ranked against others *in that state*; the national file ranks against the country, same columns, different numbers. Loaded: county 360 rows (0% null), tract 7,640 (0.3% null). Milwaukee County went from 19 to 24 indicators [source: orchestrator]

- [ ] **FCC Broadband: URL wrong AND the host blocks automated downloads.** Two separate problems. (1) `fetch_fcc_broadband.py:47` points at `fixed_broadband_deployment_data_jun2024.csv`, which does not exist — the real Form 477 county files are ZIPs (`county_connections_200906_202506.zip`, `county_tiers_201406_202506.zip`) and the program is **not** retired; data runs through **June 2025**. (2) `www.fcc.gov` returns `403 Access Denied` to every programmatic request — tested from two different IPs, with browser User-Agents and a matching Referer. It requires a real browser session. Options: a manual one-time download, browser automation, or an alternative source (data.gov mirror or the BDC API at broadbandmap.fcc.gov). Needs an operator decision — automated ingest is blocked as designed by their WAF [source: orchestrator]

- [ ] **FBI NIBRS: blocked on a credential.** `fetch_fbi_nibrs.py` requires `FBI_CDE_API_KEY`, which is not in `/etc/pdi/env` or the local environment. Free signup at https://api.data.gov/signup/. Script and metadata are otherwise ready — 2 variables, 0 rows, waiting only on the key [source: orchestrator]

- [ ] **EPA EJScreen base URL is dead.** `gaftp.epa.gov` was taken offline by EPA in Feb 2025. The Python path now uses a Zenodo mirror (`zenodo.org/records/14767363`), which is manual-update-only for 2024+ vintages. Any Go path still pointing at gaftp will fail [source: sources.toml audit]
- [ ] **`fbi_nibrs` is registered but returns no data.** `FetchCounty`/`FetchState` are unwired TODO stubs, so the source appears in `Registry.All()` and looks available while producing nothing even with a valid `FBI_CDE_API_KEY` [source: verified at `pkg/datasource/fbi_nibrs.go:129,169`]
- [ ] **5 of 16 registered adapters return no data through their Go interface**: `tiger`, `epa-ejscreen`, `cdc-svi` (explicit 501s — real path is Python, which is fine but undocumented until now), `fbi-nibrs` (stub), `fcc-broadband` (silent nil). Now documented per-source in `data/sources.toml` [source: sources.toml audit]

- [x] ~~**Theme toggle renders nowhere.**~~ — **NOT A DEFECT as of 2026-07-29.** Re-verified by measuring the rendered DOM on all 10 pages in headless Chromium: `theme-toggle.js` and `lang-toggle.js` are each loaded on 10/10 pages and *both buttons render on all 10*. The `es/` twin directory is also gone. ADR-013 §3C and ADR-011 §1C are satisfied. The original claim was reasoned from the shared script layer rather than measured per page — the exact inversion `pdi-verify-per-page-not-shared-layer` warns about [source: orchestrator, measured 2026-07-29]
- [ ] **Tract geometry/data vintage mismatch — the real "map quality" root cause.** Three counts are live at once: `tracts.geojson` (what the map draws) = **1,542**; live PostGIS (what LISA is computed on) = **1,669**; LISA analyses = **1,524/1,525** scores. So ~18 rendered tracts have no LISA score and ~127 DB tracts have no geometry. Presents as "the map looks patchy." Fix = reload tract geographies at ACS 2024, then recompute LISA. **Corrects the number recorded below**: the DB is at 1,669, not 1,652 [source: orchestrator, measured via offset paging on the live API]
- [x] ~~**`total` field breaks pagination.**~~ — **NOT REPRODUCIBLE as of 2026-07-29.** Re-measured against live at limit 1 / 5 / 100 / 1000: `total` returns **1542 every time** for tracts and **72** for counties — the unfiltered count, never the page size. `count` correctly reports the page size separately. Either this was fixed, or the original reading conflated `count` with `total`. The scale strip's live hydration now depends on `total` being authoritative, so this is exercised on every landing-page load [source: orchestrator, measured 2026-07-29]

## P0 — Found 2026-07-30 (UI/UX spike salvage)

- [x] ~~**The homepage was rewritten to publish fabricated county statistics.**~~ — REVERTED 2026-07-30. An uncommitted `index.html` rewrite (21:02) imported the content model *and the numbers* from `~/pdi-spike-direction-b`, a spike that self-certified "✓ COMPLETE / Real PDI Data Included" and was fabricated end to end. Every checked figure was wrong against the platform's own 72-county dataset: Ashland poverty 18.2% (actual **13.4**), Ozaukee 6.1% (**5.0**), Menominee uninsured 9.8% (**20.5**), Burnett 4.2% (**6.0**), Sheboygan rent burden 37.2% (**31.0**), Milwaukee 35.5% (**52.8**). The narratives were inverted, not merely imprecise — it named Ashland the state's poverty leader (real leader: Menominee 21.7%) and Menominee the uninsured leader (real leader: Clark 22.5%). Six of its ten advertised variables — Transit Access, Life Expectancy, Unemployment Rate, High School Graduation, Broadband Access, Food Insecurity — **exist in no PDI data source at all**, so "Clark improved 28% → 41.2% transit access" describes a measurement never collected. The page also had zero `fetch`/`/v1/policy` calls (every number hardcoded) and dropped all 49/50 `data-en`/`data-es` pairs, leaving the homepage English-only on a site with `/es/` routes. Reverted to HEAD; both versions preserved off-repo. Spike B is quarantined with `00-DO-NOT-USE-FABRICATED.md` [source: orchestrator, verified against `explorer-data.js` 72-county dataset]

- [ ] **Rebuild the card-led homepage direction against real data (the salvageable half).** The reverted rewrite's *structure* — 4 narrative insight cards + "Explore All Variables" — is a legitimate UX direction and is worth redoing properly; only its content was fabricated. Constraints for the rebuild: (a) every figure must come from the API or the embedded ACS snapshot, never a literal; (b) only the 10 fields that actually exist may be shown — `median_hh_income`, `poverty_rate`, `pct_cost_burdened`, `pct_severely_cost_burdened`, `uninsured_rate`, `pct_bachelors_or_higher`, `pct_poc`, `pct_hispanic`, `pct_non_hispanic_black`, `total_population`; (c) EN/ES parity is non-negotiable (49/50 pairs today); (d) superlatives ("highest in Wisconsin") must be *derived* at runtime, since that is exactly the claim class the spike got backwards. Prior art preserved at `index.html.afternoon-rewrite-2026-07-30` [source: orchestrator, 2026-07-30]

- [x] ~~**The local database was reset mid-session and never re-seeded.**~~ — DONE 2026-07-31. Reseeded at **ACS 2020-2024 5-Year** (resolves ADR-014 open question 2; the DB being empty made the "more correct" option free). Live: **1,614 geographies** (72 counties + 1,542 tracts, TIGERweb `tigerWMS_ACS2024`) and **21,568 indicators** — acs-5yr county 1,368 (19 vars × 72, 100% non-null, **all 1,368 carrying an MOE**), cdc-places tract 12,200, cdc-svi county 360 + tract 7,640. 14 sources and 41 indicator_meta rows registered. `indicators_latest` refreshed. **USDA remains unloaded** — see the new P0 item below. The reseed surfaced four defects that made it impossible rather than merely tedious, each fixed and committed separately: nothing in the repo registered `indicator_meta` for the four loaded sources (`0c621da`..`076edbf`), `PutIndicators` could not write the `reliability` enum, `indicators_latest` never exposed `cv`/`reliability` (`7dae5ca`), and a zero-row fetch reported `ok` (`73ac948`) [source: handoff 2026-07-31_pdi-data-layer-buildout]

- [ ] **P0 — USDA Food Access is keyed to 2010 census tracts; the platform is on 2020 tracts.** Found 2026-07-31 when the load failed on `indicators_geoid_fkey` after fetching 6,960 records. USDA FARA 2019 is the newest published release and there is no 2020-tract version. Measured against TIGERweb: WI has 1,409 tracts at the 2010 vintage vs 1,542 at 2020, and **127 (9.0%) of the 2010 GEOIDs have no 2020 counterpart**. The other 91% match by identifier only — a tract that kept its code through a boundary revision would load *silently* and wrongly, which is worse than the 127 that fail loudly. Options: load the 91% and record the 127 dropped; withhold until an NHGIS 2010→2020 crosswalk is wired in; or carry USDA at county level only. ADR-014 D2 (absent beats caveated-wrong) argues against the first. **Blocked on operator decision** — recorded as ADR-014 open question 6 [source: orchestrator, measured 2026-07-31]

- [ ] ~~superseded — see the two items above~~ **The local database was reset mid-session and never re-seeded.** `policy-data-infra-postgres-1` was **created 2026-07-30 18:34:24 local**, inside the same window as the UI spike work (spikes 18:15–18:20, repo edits from 18:26). The volume came up with schema but almost no data. Measured against the CLAUDE.md benchmarks:

  | Table | Expected | Actual |
  |---|---|---|
  | `geographies` (county) | 72 | **0** |
  | `geographies` (tract) | 1,542 | **0** |
  | `indicators` | ~1,368 ACS + ~12,200 CDC + ~8,009 USDA | **0** |
  | `indicator_sources` | ≥4 | 3 (`cdc-svi`, `fbi-nibrs`, `fcc-broadband`) |
  | `indicator_meta` | ≥20 | 9 |

  Note the 8,000 CDC SVI rows loaded on 2026-07-30 (item above) are **gone** — same reset. Only steps 2–3 of the FK chain (`geographies → indicator_sources → indicator_meta → indicators`) survived, and because `indicators` is FK-dependent on `geographies`, **no indicator load can succeed until geographies is seeded first** — a bare re-run of the fetchers will fail on FK violation, not on the network. Diagnosis only; nothing was re-fetched. Remediation order:

  ```
  go run ./cmd/pdi migrate up                  # confirm schema current
  # 1. seed geographies FIRST (county then tract) — everything else FKs to this
  # 2. re-register indicator_sources + indicator_meta for census/tiger/cdc-places/usda
  # 3. reload indicators (CDC PLACES + USDA can run concurrently)
  REFRESH MATERIALIZED VIEW CONCURRENTLY indicators_latest;
  ```
  Overlaps the queued `2026-07-31_pdi-data-layer-buildout` handoff — fold this in rather than duplicating it. Until it runs, `/explorer` correctly shows the embedded ACS snapshot only [source: orchestrator, measured 2026-07-30]

- [x] ~~**`/v1/policy/sources` returns a hardcoded list of 9, not the rows actually in `indicator_sources`.**~~ — DONE 2026-07-31 (`8fb0930`). The endpoint now serves three distinct counts, because they are three different questions: `total` 9 (adapters compiled in — stays 9 against an empty DB), `sources_loaded` 6 (sources with variables described in `indicator_meta`), and the new `sources_with_data` 3 (sources with at least one stored indicator row). Added `Store.CountSourcesWithData` for the last. The `/explorer` hero badge reads "data sources" next to a map of real county data, so it now hydrates from `sources_with_data` — **3, previously 9** — with a `_ok` flag so a failed lookup keeps the "—" placeholder rather than rendering a number we can't stand behind. Verified against the live API [source: orchestrator]

- [ ] ~~superseded~~ **`/v1/policy/sources` returns a hardcoded list of 9, not the 3 rows actually in `indicator_sources`.** `handleListSources` (`pkg/gateway/handlers.go:816`) returns a static Go slice of supported *adapters*; the DB holds `cdc-svi`, `fbi-nibrs`, `fcc-broadband` only. Any UI that hydrates a "data sources" figure from this endpoint reports 9 while 3 have loaded data. The explorer hero now derives from it (better than a hand-typed literal, which is what it replaced) but inherits the overcount. Either serve the DB count or rename the field to "sources supported" [source: orchestrator, verified 2026-07-30]

## P1 — High (found 2026-07-29, frontend usability pass)

- [x] ~~**Landing-page scale strip hardcoded its four headline figures.**~~ — DONE 2026-07-29. `data-count="42"` variables against a live **51**, and `13` sources against a live **9**; counties (72) and tracts (1,542) were correct. The meta description repeated both wrong numbers, so they were in search snippets too. All four now hydrate from the API (`hydrateScaleStrip`, bottom of index.html), with the literal demoted to a ship-time fallback for JS-off visitors and API outages. The meta description — the one figure that genuinely cannot hydrate, since crawlers read it pre-script — was reworded to rounded, hedged counts ("1,500+ tracts", "50+ indicators") so a routine data load can't silently falsify it. Verified by serving deliberately-wrong literals and confirming hydration corrects both text and `data-count` [source: orchestrator]

- [x] ~~**`FEATURED_POOL` hardcodes statistics that will rot** (`index.html:841`).~~ — DONE 2026-07-29, and it was worse than stale: `geoid '55141'` was labelled "Menominee County" but 55141 is **Wood County** (Menominee is 55078), so one day in three the card linked to the wrong county. All stats now derived at runtime; four external claims fact-checked against BEA / Census / USDA ERS and all four found wrong, then corrected and cited inline on the card. Original note follows for context: Carries literal `population: 916205`, `tract_count: 297`, and prose asserting "26% of its residents", "12 points", "2.6× the state average", "$100B+ GDP", "48,000 residents". This is the **same failure class** as commit `dc84998` ("derive the high-uninsured counties instead of hardcoding stale ones"), where hardcoded figures were 6 points off and being presented as grounded fact on a public policy tool. The featured card already loads live county data — derive the numbers from it and keep only the qualitative prose [source: orchestrator, 2026-07-29]

- [ ] **Mobile nav hides 7 of 9 links by default** (design call, not a layout bug). At 375px `.site-nav` is `flex-wrap: nowrap` with `min-width: 0` + `overflow-x: auto` — a deliberate scroll rail (documented at `styles.css:414-457`) that absorbs its own overflow so it can never push the page into horizontal scroll. It works as designed: **no page overflows at 375px**. But only "Explorer" and "Evidence" are fully visible, "Candidates" is clipped mid-label, and the remaining 6 are off-screen, reachable only by swiping inside the rail with no affordance indicating more exists. Options: scroll-hint gradient, a disclosure/menu pattern, or accept as-is. Corrects TODO's earlier "no flex-wrap handling, may overflow" framing — half right, wrong conclusion [source: Track C, measured on all 9 non-county pages]

- [x] ~~**Cold empty states on the tool pages**~~ — DONE 2026-07-30. Aggregate-first: the unselected state now shows what the tool knows, and selecting *narrows* it. `/compare` renders statewide distribution strips across all 72 counties on arrival — the same `.dist-strip` visualization the loaded state uses, so selecting two counties adds two marks to a picture already on screen rather than replacing it — plus derived entry points ("Ozaukee County vs Menominee County — widest gap in Poverty Rate"). `/composite` shows the live variable catalog and a worked micro-example; `/chat` shows what it can answer and closed a 542px dead gap. **Auto-populating a default county pair was explicitly rejected**: a default silently asserts a canonical comparison, which on an equity platform is an editorial claim, not a neutral one. **Metric decided:** extremes on a single *named* indicator, not an aggregate standardized gap — an aggregate hides its own weighting behind a number nobody can audit, while extremes are defined by an axis the user can see. No reliability floor: any population threshold is itself an undeclared editorial choice, and the comparison surfaces reliability/materiality downstream where uncertainty is actually shown. Every label generated from live data; no hand-written string carries a number or superlative [source: orchestrator scout + 2 tracks]

- [ ] **`/composite`'s direction filter is silently broken and always returns zero.** The Adversity/Resilience dropdown (`composites.html:925-926`, logic `:1421-1422`) filters on `v.direction === 'adversity'` / `'resilience'`, but the live API's actual values are `lower_is_better` (33), `neutral` (12), `higher_is_better` (4), missing (2). Selecting either option always yields an empty list. The same mismatch makes the per-variable direction badge (`:944-949`) render the raw enum ("lower_is_better") instead of a styled label, and never match the `.direction-adversity` / `.direction-resilience` CSS. Same class as the `v.variable_id` vs `v.id` bug that made /candidates report 0% coverage — a frontend reading a field name the API doesn't use [source: empty-states track]

## P1 — High (grant-critical)

## P0 — Found 2026-07-29 during the i18n pass (pre-existing, not i18n regressions)

- [x] ~~**`/compare` overflows horizontally by 1,216px at 375px once a comparison is rendered.**~~ — DONE 2026-07-30. The flex/grid theory in the original note was **wrong**; the warning attached to it (check for county.html's pattern first) was right. Measured cause: exactly ONE leaking element, a `<table class="visually-hidden">` rendering at **1,563px**. `.visually-hidden` sets `width:1px`, but a `<table>` under `table-layout:auto` derives its width from column min-content and discards that — so an invisible, absolutely-positioned table was pushing the document 4× the viewport. **Third instance of the same defect** (county 118px, composites 193px, compare 1,216px); county and composites each carried a *private copy* of the fix in their own `<style>` block, which is exactly why compare.html — written later — reproduced it without inheriting either. Fixed centrally in `styles.css` beside the `.visually-hidden` definition (that file is PDI-owned, 2,809 lines diverged from casa-datos), and the two duplicated local copies were removed. Both rules are required: `table-layout:fixed`, plus `white-space:normal` on caption/th/td, because `.visually-hidden`'s `white-space:nowrap` inherits and a table cannot compress below the min-content width of its widest unbreakable run. Verified 24 combinations (3 pages × 320/375/414/768 × en/es), and the new check was confirmed to fail against the reverted fix [source: orchestrator]

- [x] ~~**Audits measured interactive pages only in their empty state.**~~ — DONE 2026-07-30, the generalization behind the bug above. Added `scripts/layout-check.mjs`: 80 checks across 10 pages × 4 widths × 2 languages, driving `/compare` to results, `/composite` through Compute and `/map` through a tract click **before** measuring. It reports the element that actually *leaks* — one extending past the viewport with no clipping ancestor — rather than every element past the edge, which is what sent the first two investigations chasing nav links inside the deliberate `.site-nav` scroll rail. Convention recorded in CLAUDE.md [source: orchestrator]

- [ ] **`/map`'s tract-click mini-profile panel is invisible to every user, in every language.** CSS declares `.mini-profile-panel { display:none }` / `.mini-profile-panel.visible { display:block }`, expecting a `.visible` class toggle — but the markup uses Alpine's `x-show="mpVisible"`, which sets inline `display` and never adds that class. Verified via Alpine state inspection: `mpVisible` becomes `true` and `mpTract` populates correctly on click, but `getComputedStyle` stays `display:none`. So clicking a tract on the map appears to do nothing. Pre-existing and language-independent (map.html:482, 496, 726) [source: Track A]

- [x] ~~**`lang-toggle.js` cannot translate `aria-label` / `title` attributes.**~~ — DONE 2026-07-29. `swapDOM` now handles `data-<lang>-<attr>` for `aria-label`, `title`, `alt` and `placeholder`; all 45 sites (38 aria-label + 7 title) across the 10 pages carry pairs [source: i18n pass]
- [x] ~~**Alpine `x-text` output is not translatable.**~~ — DONE 2026-07-29. Fixed with a reactive Alpine store (`$store.i18n.t()`) registered by `lang-toggle.js` before Alpine's first pass, plus a MutationObserver so late-rendered `data-en`/`data-es` pairs are swapped too. Measured EN-only strings per page after the pass: index 0, compare 0, map 0, narrative 0, chat 0, composite 2, county 6, candidates 33, about 40, evidence 47 — **every remaining one is API/JSON-derived data** (evidence-card prose, variable names, source names), verified individually. Markup-level residual is **0 on all ten pages**. Original item follows:
- [ ] ~~superseded~~ **Alpine `x-text` output is not translatable.** All runtime-generated strings bypass `swapDOM` entirely and stay English in Spanish mode: card titles/findings/categories (evidence), policy + dimension fields (candidates), method and stability labels (composites), source/variable/reference tables (about). Also several strings with English hardcoded *inside* the Alpine expression — e.g. `dim.policies.length + (…===1 ? ' policy' : ' policies')`, `:title="ind.available ? 'Data available in PDI' : …"`, `v.source_name + ' · ' + (v.unit || 'index')`, and two "No results" ternaries in about.html. Needs a JS-level i18n layer, not attribute pairs [source: Wave 2B]
- [x] ~~**Reconcile "evidence card" / "composite index" in Spanish copy.**~~ — DONE 2026-07-29. House rule applied consistently across all pages: product nouns translate (`tarjeta de evidencia`, `índice compuesto`); genuine terms of art stay English (ICE, LISA, z-score, GEOID, NARI, ACS, and the five LISA cluster designations incl. "Not Significant"). Original item follows:
- [ ] ~~superseded~~ **Reconcile "evidence card" / "composite index" in Spanish copy.** 21 strings currently read as Spanglish ("No hay evidence cards que coincidan con tus filtros", "Constructor de Composite Index") because the dispatch instruction wrongly classified these product nouns as untranslatable domain terms of art alongside z-score/ICE/LISA. Decide the house rule and apply it consistently: `tarjeta de evidencia` / `índice compuesto` are the natural translations. Genuine terms of art (ICE, LISA, z-score, GEOID, NARI) correctly stay English [source: Wave 2B flagged it; orchestrator's instruction caused it]
- [ ] **Spanish copy needs a native-speaker review pass.** Specific flags: "Std Dev" → "Desv. Est." (unverified abbreviation), "Quantile Transformation" → "Transformación por Cuantiles", "Ranking stability" → "Estabilidad del ranking", nav "Evidence"→"Evidencia" / "Composites"→"Compuestos", and "seeded" translated loosely in "Aún no se han agregado candidatos" [source: Wave 2B]
- [x] ~~**about.html body prose remains English.**~~ — DONE 2026-07-29. Overview paragraphs, all 5 Architecture layer blocks and all 6 Methodology method-cards are now bilingual (48 new pairs). Also fixed 4 pre-existing `data-es` attributes that contained literal `\u00e1`-style escape sequences — never interpreted in an HTML attribute, so Spanish mode was rendering the raw backslash-u text. Original item follows:
- [ ] ~~superseded~~ **about.html body prose remains English.** Headings, eyebrows, section intros, tables and the Funding/License sections are bilingual; the Overview paragraphs, all 5 Architecture layer blocks, and all 6 Methodology method-cards are not — deliberately deferred as the highest mistranslation-risk, lowest-first-scroll-visibility content. This is the bulk of the file's word count [source: Wave 2B]

- [ ] **`data/sources.toml` is 12 sources out of date.** It lists 5 (tiger, hmda, osm, hrsa, gtfs) against **17 Go adapters** and **12 Python ingest scripts** on disk. Missing: acs, bls_laus, cdc_places, cdc_svi, epa_ejscreen, epa_tri, fbi_nibrs, fcc_broadband, hud_chas, hud_pit, usda_food, wi_dpi. The repo's own "add a new source" checklist and the `api_key_env` lookup both treat this file as authoritative — it describes ~30% of reality. Blocks map-data-source expansion [source: orchestrator]
- [x] ~~**Emit ACS margins of error for all ACS-derived indicators.**~~ — DONE 2026-08-08. The Go county path already carried 19/19 after the reseed; the Python path was the gap, and `fetch_acs.py` was rewritten to the canonical vocabulary with an MOE companion for every variable (ADR-014 D4/D5). Tract load verified: 29,242 of 29,242 non-null values carry an MOE [source: handoff 2026-08-02, `cb69697`]
- [ ] **Add `direction` + `unit` to the `differences[]` array** in the compare API response. It currently omits both, so any client trusting that array alone will colour deltas without polarity — exactly what ADR-013 §2A calls misleading [source: Track D]
- [ ] **Verify polarity on `owner_/renter_cost_burden_30pct_1..5`.** All carry `lower_is_better` with an *empty* description, but the values look like ACS bracket components/denominators rather than outcome rates (Dane `renter_cost_burden_30pct_1` = 104,037 against 245,736 total housing units). They occupy 10 of 13 scoreboard slots and therefore dominate the Compare headline. Either document them or re-mark the components `neutral` [source: Track D]

## P1 — High (grant-critical, pre-existing)

- [ ] Verify `ingest/fetch_tiger.py` against a live PostGIS. The TIGERweb repoint is verified only via `--dry-run` (no psycopg on the build machine); `normalize_props()` -> `bulk_load_geographies()` has not been exercised against a real DB [source: PIP-91]
- [ ] Reload PostGIS tract geographies at the ACS 2024 vintage — the DB predates this and the tract count on record (1,652) does not match the verified 1,542 [source: PIP-91]
- [ ] Confirm whether `block_group` supports a statewide Census query. `lib/census._geo_clause()` still requires a county there; only `tract` was relaxed and verified [source: PIP-91]

- [ ] Build policydatainfrastructure.com — domain returns 200, needs real app content beyond static marketing page [source: roadmap]
- [ ] MCF LOI draft review with Justice — 9 open questions, due Jun 3 2026 (49 days) [source: grant]
- [ ] Arnold Ventures $591K — decision ~May 2026, no action required [source: grant]

## P2 — Medium (quality, testing)

- [ ] **`pkg/store/postgres.go:272` uses `err == pgx.ErrNoRows`** instead of `errors.Is(err, pgx.ErrNoRows)`. Direct equality misses any wrapped error, so a "not found" that passes through a wrapping layer presents as a generic failure. Violates the repo's own rule in CLAUDE.md § Security [source: Track D, flagged outside its ownership]
- [ ] **Dead `.hero` / `.hero-subtitle` CSS in `index.html:443-460`** targets a `<div class="hero"><h1>…<p class="hero-subtitle">` structure that no longer exists in the file (zero matches for either class). Inert today, but it's a name collision waiting to bite — adding `class="hero"` to a wrapper would silently pick up a stale 700-weight h1 rule at a different clamp size than the live `.hero-statement` [source: Track C]


- [ ] **`.site-nav` has no `flex-wrap` / mobile handling** and the nav just grew from 4–6 links to 9 on every page — verify it doesn't overflow on narrow viewports [source: Track B]
- [ ] **Decide `--grad-cta`.** ADR-013 §2B describes it as sun→ember→red; in dark theme the token actually renders amber→pink→purple (`#fcd34d → #f472b6 → #8b5cf6`). The token is byte-identical to casa-datos and casa-datos's own `.btn-primary` uses it, so it was left alone. Fixing this by editing `tokens.css` would break token fidelity with the reference system — decide at the ADR level [source: Track A]
- [ ] **Add a distribution/rank endpoint** (`GET /v1/policy/distribution?variable_id=&level=&state=`). `pkg/stats` already has `PercentileRank`, `ZScore`, `CoefficientOfVariation`, `ReliabilityLevel`. Would cut Compare's context request from ~225KB to ~10KB and move the math into the tested Go engine instead of a JS reimplementation [source: Track D]
- [ ] **Add `category` to `/v1/policy/variables`** — every page currently falls back to keyword-guessing in `inferCategory()` [source: Track D]
- [ ] **Full ADR-013 §1B section-band adoption on the 6 div-based pages** needs a dedicated content-architecture pass (those pages have zero `<section>` elements). Do this after the new `.section-band`/`.thread` CSS is visually verified on `about.html`/`index.html` [source: Track B]
- [ ] Orphaned `.chat-toggle` CSS remains in `composites.html` with no corresponding button (pre-existing) [source: Track B]
- [ ] `about.html`'s local `.about-section { margin-bottom: var(--section-gap) }` now stacks with the new `.section-band` padding on the same elements — check for doubled spacing [source: Track B]
- [x] ~~Dead element in county.html Levers section: `<span class="layer-indicator li-1" x-show="false">`~~ — DONE 2026-07-29. Confirmed genuinely dead (`x-show="false"` is a literal, not a bound expression; empty, no children, no selector anywhere targets it, and it was the file's only `x-show="false"`). Removed; before/after rendering identical [source: Track B]
- [ ] Add `denominator_variable_id` to indicator metadata — count indicators currently normalize against `population`, but housing measures should use housing units [source: Track D]

- [x] ~~Add tests for `pkg/store/`~~ — DONE: 15 integration tests, 0% → 77.5% (`19c1242`)
- [x] ~~Add post-stage validation gate to pipeline~~ — DONE: ValidateStage + Config.Validate() (`19c1242`)
- [ ] Audit transitive deps: `go mod why github.com/quic-go/quic-go` and `go.mongodb.org/mongo-driver` [source: health-audit]
- [ ] `pkg/pipeline/` coverage at 30.4% — add stage-level unit tests [source: audit]
- [ ] `pkg/gateway/` coverage at 41.2% — add handler tests [source: audit]

## P3 — Low (polish, national-scale)

- [ ] Wire WI DPI attendance data into Go pipeline (currently Python-only) [source: architecture-gap]
- [ ] National-scale data pipeline (all 50 states) — 13 adapters ready, need orchestration + rate limit budget [source: roadmap]
- [ ] GTFS + EPA-TRI end-to-end tests with mock HTTP servers [source: adapter-audit]
- [ ] HTTP 500 error handling tests across all adapters [source: adapter-audit]
- [ ] HTMLCraft v3.5 Polish: animations, shortcuts, ARIA [source: session]

## Backlog — Future Phases

- [ ] Gateway protocol module merge (2→1) [source: deferred]
- [ ] Factor analysis pipeline integration (currently Python-only) [source: analysis]
- [ ] Tract-level EPA TRI attribution via PostGIS ST_Within [source: adapter-limitation]
